// Simple local font implementation that doesn't use next/font
// This helps avoid the font module errors

// Export font class names for use in components
export const fontClasses = {
  sans: 'font-sans',
  mono: 'font-mono',
}; 